tabfact-small_test测试结果

ChatGPT的结果是完整的small-test测试集，共2024个，与tabfact原数据保持一致，评测时需要去掉带等号的26个样例
LLaMA结果是json文件，每个元素是一个dict，_id是样例ID，"_"后的数字是按照完整的2024个样例递增的，结果已经排除了带等号的样例，所以在结果中后面的数字不一定连续
ChatGPT结果是tsv文件，每行的第一个元素是ID号（包含了带等号的样例），与LLaMA结果中的_id后面的数字保持一致，第二个元素是请求返回的结果
